export enum Yad1Path {
  Feed = '/feed',
  Projects = '/projects',
  Map = '/map',
  Listings = '/listings',
  Developers = '/developers',
  DeveloperFeed = '/developers/feed',
  ProjectsAutocomplete = '/projects/autocomplete',
}

export enum MarketPath {
  Search = '/search',
  Filters = '/filters',
  MenuItems = '/menu-items',
  Autocomplete = '/search/autocomplete',
}

export enum AddressPath {
  Regions = '/regions',
  TopAreas = '/top-areas',
  Areas = '/areas',
  Cities = '/cities',
  Hoods = '/hoods',
  Streets = '/streets',
  Autocomplete = '/autocomplete',
}

export enum NeighborhoodPath {
  Suggestions = '/suggestions',
}

export enum NearbyPath {
  NearMe = '/near-me',
}

export enum OptionsPath {
  Base = '/base',
  Dynamic = '/dynamic',
}
